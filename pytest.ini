[pytest]
markers =
    debug: execute debug tests
    smoke: execute smoke tests
    macaco: just for debug
    cont_test: used for github actions
    
log_cli = true
log_cli_level = INFO
log_cli_format = %(asctime)s %(levelname)s %(name)s: %(message)s
log_cli_date_format = %Y-%m-%d %H:%M:%S